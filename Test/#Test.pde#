PImage map;

void setup(){
  size(640,572);
  //map = new Map("NewBarkTown");
  map = loadImage("NewBarkTown2.png").get(1192,12,1536,1152);
  image(map,0,0);
  test();
}

//960x304

void draw(){
  
}



//287 is where the green starts
void test(){
  map.loadPixels();
  println(map.pixels.length);
  color c = map.get(map.width-1,map.height - 1);
  fill(c);
  rect(900,900,50,50);
  /*
  for(int y = 303; y > 280; y--){
    color c1 = map.get(0,y);
    if(!(c1 == c)){
     println(y); 
    }
  }
  */
  
}
